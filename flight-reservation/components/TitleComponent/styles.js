import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  title: {
    color: "#6270de",
    fontWeight: "900",
    fontSize: 28,
    paddingBottom: 10,
    letterSpacing: .9
  },
});

export default styles;
