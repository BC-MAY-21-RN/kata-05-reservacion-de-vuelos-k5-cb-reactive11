import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  title: {
    color: "#6270de",
    fontWeight: "800",
    fontSize: 24,
    paddingVertical: 30,
  },
});

export default styles;
